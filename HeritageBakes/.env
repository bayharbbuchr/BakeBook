# Local development
VITE_PUBLIC_URL=/
VITE_API_URL=http://localhost:5001/api

# Production (Vercel will replace these with production values)
# VITE_PUBLIC_URL=/
# VITE_API_URL=${VERCEL_URL ? `https://${VERCEL_URL}/api` : 'http://localhost:5001/api'}
